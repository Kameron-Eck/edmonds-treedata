"""litkb.acquire — acquisition routes and the store (design §10): open access (paper-search as a library),
Anna's Archive (the aa_fetch gates, ported), Sci-Hub by DOI, and the browser route recorded as a manual step.
Every attempt is written through litkb.record_acquisition_attempt; no module here holds a delete path."""
