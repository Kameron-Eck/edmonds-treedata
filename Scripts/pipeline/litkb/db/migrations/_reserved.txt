# Migration numbers claimed by a branch that has not merged here yet (litkb.db.migrate.reserved).
#
# A gap in the migration numbering is normally a LOST migration and is refused. A number listed
# here is a gap that is expected: another open branch is writing it, and renumbering around it
# would put two different migrations on one number at merge. Delete the line in the merge that
# lands the file.
#
# NNNN  why

0018  work/20260915-access-layer: 0018_access_layer.sql
0019  work/20260915-access-layer: 0019_prepare_requires_evidence.sql

# COLLISION, for whoever lands 0018. It CREATE OR REPLACEs litkb._feeds_token_ok, and so does
# 0020_first_use_friction.sql — both widening it from three of the convention's feeds forms to all
# seven, written independently on the two branches. The bodies are NOT identical: 0018 allows
# `framework §N[.N]` at most one sub-level (which is what the convention's own table says), 0020
# allows any depth. And because a CREATE OR REPLACE is decided by the order migrations were APPLIED
# rather than by their numbers, a database that already has 0020 (litkb does) ends up with 0018's
# body, while one built from scratch runs 0018 then 0020 and ends up with 0020's — the same
# migration set, two different schemas. 0020 is applied and checksum-locked, so it cannot be the one
# that changes; either 0018 drops its section before it is applied anywhere, or a later migration
# states the definition once. Kam decides. Reports/LITKB_P3_REPORT_2026-09-15.md, "First-use
# friction closed" -> Coordination.
