# Migration numbers claimed by a branch that has not merged here yet (litkb.db.migrate.reserved).
#
# A gap in the migration numbering is normally a LOST migration and is refused. A number listed
# here is a gap that is expected: another open branch is writing it, and renumbering around it
# would put two different migrations on one number at merge. Delete the line in the merge that
# lands the file.
#
# TWO KINDS OF LINE LIVE HERE, and until 2026-09-20 this header only described the first:
#   IN FLIGHT   a branch is writing that migration. The line goes away with the merge that lands it.
#   RETIRED     the branch merged WITHOUT a migration, so the number was never written and never
#               will be. The gap is permanent, the line is permanent, and the number is never
#               reused — renumbering the files above it would rewrite migrations that other
#               branches, instruments and reports already name. A retired line is marked RETIRED
#               and says what happened.
# The runner does not distinguish them (litkb.db.migrate.reserved parses `NNNN why`), and it does
# not need to: both are a declared gap. The distinction is for the human deciding whether to delete
# the line.
#
# NNNN  why

0024  RETIRED — reserved 2026-09-20 for work/20260920-web-source-gate, merged without a migration; the number is retired, never reuse it
0032  IN FLIGHT - S4.5 (ladder-1) identifier model: scheme_registry, identifier provenance, parent-work columns (LITKB_WORKPLAN.md S4.5 item 1)
0033  IN FLIGHT - S4.5 (ladder-1) acquisition ledger vocabulary: attempt sub_status/terminal/retriable/kind/served sha, route back-off state (S4.5 items 1-2)
0034  IN FLIGHT - S4.5 (ladder-1) adjudication: refuse verb, decision log, withdraw_version, operator-bind gate (S4.5 item 1)
#
# 0027 is no longer reserved: it landed on work/20260920-litkb-s1a as
# 0027_hunt_request_title_scheme.sql (hunt_requests.ref_scheme gains 'title'; LITKB_WORKPLAN.md
# S1), and a number on disk must not also be reserved
# (qc/test_litkb_p1.py::test_migration_files_are_named_and_numbered). It replaces one CHECK
# constraint and no function body, so the two-branches-one-function hazard below does not apply
# to it — checked rather than assumed, 2026-09-20: `git ls-tree` over every local and remote
# work/* and fix/* ref found no migration numbered 0024 or above on any other branch.
# 0025 is no longer reserved: it landed on this branch (work/20260920-index-normaliser) as
# 0025_search_index_ligature_expansion.sql, and a number on disk must not also be reserved
# (qc/test_litkb_p1.py::test_migration_files_are_named_and_numbered). It CREATE OR REPLACEs
# litkb.norm_search_text, which is the two-branches-one-function hazard the note below describes, so
# it was checked rather than assumed: on 2026-09-19 `git grep norm_search_text` over the migrations
# directory of every local and remote work/* and fix/* ref found that function in 0018 and in this
# file only. Nothing else open replaces it, so apply ORDER cannot produce two different bodies.
#
# 0018 and 0019 landed 2026-09-16 with the merge of work/20260915-access-layer, so their lines are
# gone: a number on disk must not also be reserved
# (qc/test_litkb_p1.py::test_migration_files_are_named_and_numbered).
#
# The COLLISION recorded here for whoever landed 0018 is CLOSED, by the second of the two options it
# offered. 0018 and 0020 both CREATE OR REPLACE litkb._feeds_token_ok with different seven-form
# bodies, and a CREATE OR REPLACE is decided by APPLY order rather than by number, so the same
# migration set produced two different schemas. 0018 could no longer drop its section (it is applied
# and checksum-locked, as 0020 already was), so `0021_feeds_validator_final.sql` states the
# definition ONCE and, being the highest number, is applied last on every database whatever order
# the earlier two arrived in. Its header carries the reasoning and the live measurement of the
# divergence. Reports/LITKB_P4_MERGE_2026-09-15.md, "P8 merged".
#
# 0028 was taken here 2026-09-20 with nothing to reserve, on the same rule as 0023 below: the file
# lands in the SAME commit that would have claimed the number, and a number on disk must not also be
# reserved (qc/test_litkb_p1.py::test_migration_files_are_named_and_numbered) — so reserving it and
# deleting the line in one commit would only have made the two disagree for the length of that
# commit. Checked rather than assumed: `git log --all --diff-filter=A --name-only` over this
# directory lists every migration file ever added on any local or remote ref, and the highest is
# 0027. 0028_acquisition_event_hunt_url.sql replaces one CHECK constraint
# (acquisition_attempts_route_check) and no function body, so the two-branches-one-function hazard
# above does not apply to it.
#
# 0023 (hunt_request) was taken here 2026-09-18 with nothing to reserve: `git branch -a` plus
# `git ls-tree` on every open work/* and fix/* ref showed none held 0022 or above (the highest any
# other branch carries is 0019, work/20260915-access-layer, already landed above). No line is
# needed for it — this note only spares the next session re-running the same check.
