"""phase4seg — extracted package for phase4_semantic_finetune."""

# The live engine version. pipeline_log stamps this into every step log
# (`version:` line) — it is preferred over the docstring marker it replaced,
# so it must be bumped with the code, alongside the git tag (CLAUDE.md rule 1).
# What the version MEANS at any moment lives in CHATLOG STATE, never here.
__version__ = "v048"
