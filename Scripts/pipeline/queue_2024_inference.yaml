# ONE-JOB QUEUE — 2024 full-city inference only (staged 2026-08-22, overhaul P11.6).
# Runs on the VM that finished queue_A_2024_2017's 2017 job:
#     python -u phase4_train_queue.py --queue queue_2024_inference.yaml
# 2024's labels/tile/train/evaluate carry OK rows from 2026-08-19, so resume skips
# them and this queue is exactly one step. Its two earlier inference attempts died
# for infrastructure reasons, never model reasons: 17:08Z the VM was reclaimed
# (keep-alive daemon killed by a session prune) at 39%; 17:55Z the first threaded
# build hit the shared-GDAL-handle race. Both left NOTHING on Drive — the verified
# write path keeps the raster in /content until the step completes.
# Expect ~45-60 min on an A100 with the threaded reader (was ~3 h serial).
- id: "2024"
  year: "2024"
  tag: citywide_rgb
  extra: ["--force-citywide"]
  why: >-
    City of Edmonds 5 cm (148736x211968 grid, shared with 2017/2022). Held-out
    IoU .7142 @5 cm from the 2026-08-19 train; only the citywide raster is missing.
  expect: >-
    prob raster ~2.5-3 GB, VERIFY:inference OK, then a job-end VERIFY. Score locally
    vs the clipped ccap_2021 reference (T3 footing) once the scorer says
    channels=rgb+chm.
