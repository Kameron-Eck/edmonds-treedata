# GENERATED from experiments/encoder_bases.yaml — DO NOT EDIT.
# Edit the experiment file and rerun:
#   py -3.12 qc/experiment_queue.py --experiment experiments/encoder_bases.yaml
# test_experiments.py::test_generated_queues_match_their_experiments fails on drift.
# ONE QUEUE PER RUNTIME (COLAB_AUTONOMY_SETUP.md); arms wanting parallel
# runtimes get split into per-arm files by hand, still generated-headered.
- id: encoder_bases_base50
  year: '2020'
  tag: base50
  extra:
  - --encoder
  - resnet50
  - --no-hillshade
  why: arm of experiments/encoder_bases.yaml — hypothesis and decision rule live THERE,
    not here.
  steps:
  - train
  - evaluate
- id: encoder_bases_base18
  year: '2020'
  tag: base18
  extra:
  - --encoder
  - resnet18
  - --no-hillshade
  why: arm of experiments/encoder_bases.yaml — hypothesis and decision rule live THERE,
    not here.
  steps:
  - train
  - evaluate
