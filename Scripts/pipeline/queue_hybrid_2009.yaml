# HYBRID LABEL ARM — 2009 (Kam: "Yeah move forward", 2026-08-28).
# Keep the projected 2020 key EVERYWHERE; change ONLY proven pixels:
# code 1 = groves + Forest site canopy (38.4 ha), code 3 = withdraw the key's
# canopy claims that verified background contradicts (20.5 ha). DOSE: 0.71% of
# the grid — label QUANTITY held constant vs baseline, only correctness moves.
# PRE-REGISTERED READ (vs baseline fullext rec .6989 / prec .8472 @0.5,
# sigma .0100): win at this dose = remarkable, closes "does correctness matter";
# null = dose too small, closes NOTHING. Single arm — the nolidar variant
# differs by ~9% of withdrawals only, not worth a second A100.
- id: "2009_hybrid"
  year: "2009"
  tag: hybrid_v1
  extra: ["--force-citywide", "--infer-aoi", "aoi/sectors_v1.json",
          "--add-canopy-mask", "/content/drive/MyDrive/treedata/phase4/labels_corrected/add_hybrid_lidar_2009.tif"]
  why: "label-correctness test at held quantity: projected key + 0.7% proven edits."
  expect: "retile ~15 min; train ~30-40 min A100; sample inference ~5 min."
