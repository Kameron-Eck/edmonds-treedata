You are running the litkb OPERATIONAL PROVING RUN, unattended. Nobody will answer questions; if a step cannot proceed, record why in the review and continue. You are in the git worktree `D:\edmonds-pipeline\wt-prove` (branch work/20260920-proving-run). You may write files only under `D:\edmonds-pipeline\wt-prove\Reports\reviews\` and the `.litkb-workstream` file the tool creates. Never `git push`. Never run `litkb review-check` yourself — a separate process grades your output.

Read first, in this order: `Scripts/docs/LITKB_REVIEW_GRAMMAR.md` (the grammar your output MUST follow, exactly), `.claude/agents/review-writer.md` (your role), `Scripts/docs/LITKB_AGENT_BASE_BRIEF.md` (how the knowledge base works).

TOPIC: "How robust is CNN semantic segmentation of aerial imagery to noisy or legacy training labels, and what does that imply for supervising other years with labels projected from one hand-labelled year?" Review slug: `label-noise-robustness-2026-09-20`.

STEP 1 — open the workstream: `litkb_ws_open(slug="proving-run-label-noise", purpose="operational proving run: label-noise robustness review, K1/K2 graded")`.

STEP 2 — drop off EXACTLY these four expectations with `litkb_hunt_request_add`, BEFORE reading any full text. Use `litkb_work` / `litkb_search` only to find each work's DOI or key for the `ref` field. Copy the `expected_claim` strings verbatim:
  a. ref: Kaiser et al. 2017, "Learning aerial image segmentation from online maps" (work key Kaiser_2017_learning-aerial-image-segmentation). expected_claim: "Deep CNNs for aerial semantic segmentation are relatively robust to noise in training labels, so large noisy label sets from online maps can replace a substantial part of manual labelling effort." why_relevant: "Our non-2020 years are supervised by labels projected from 2020; the projection error is label noise."
  b. ref: same Kaiser et al. 2017 work. expected_claim: "Kaiser et al. report that a model trained only on OpenStreetMap-derived labels OUTPERFORMS a model trained on manually labelled, pixel-accurate ground truth of the same city." why_relevant: "If true, projected labels could be preferred over hand labels; this decides whether to invest in more hand labelling."
  c. ref: MacFaden et al. 2012, "High-resolution tree canopy mapping for New York City using LIDAR and object-based image analysis" (work key MacFaden_2012_high-resolution-tree-canopy). expected_claim: "Object-based canopy mapping from lidar plus high-resolution imagery achieves canopy accuracy above 90% and is used as a reference product." why_relevant: "Defines what an accepted reference canopy product looks like."
  d. ref: Benedek et al. 2015 (work key Benedek_2015_multilayer-markov-random-field). expected_claim: "Multilayer Markov random field change detection between aerial images of different dates handles differing illumination and sensor conditions between the two dates." why_relevant: "Inter-year comparison in our archive spans different sensors and seasons."

STEP 3 — for each expectation, resolve it against the FULL TEXT: `litkb_hunt(ref=..., hunt_request=<id>)` (these works are already extracted; the call returns their state), then `litkb_search` for the relevant passages, read the blocks, and `litkb_record_use` with a VERBATIM quote from a block, `gap` = the topic question, `stance` = supports / contradicts / partial as the text warrants, and `hunt_request=<id>` so the request's resolution state can move. If the text CONTRADICTS an expectation, record the contradicting quote with stance "contradicts". If nothing in the text speaks to it, record nothing for it and leave it unconfirmed. Do NOT invent support. Aim for 6-12 recorded uses total across the four works and any others `litkb_search` surfaces on the topic.

STEP 4 — `litkb_brief()`; write `Reports/reviews/label-noise-robustness-2026-09-20.md` following the grammar exactly: header naming the workstream, claim sections where EVERY sentence ends with a citation `[work_key p.N #block_id]` whose quote is byte-verbatim from a VERIFIED line of the brief (copy from the brief; do not retype), the mandatory `## Expectations not supported` section listing every hunt_request whose state is contradicted/unconfirmed/open BY ID with its expected claim and the state (no citation tokens there), the `## Sources` table, one `## Scope` section only, no literature facts in Scope. Quotes must be ≥ 25 characters. Then stop and print the path of the review and the workstream id.
