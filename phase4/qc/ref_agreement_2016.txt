REFERENCE DISAGREEMENT MAP — year 2016
  ndvi ref : ndvi_ref_2016.tif
  ccap ref : ccap_2016_hires_lc.tif
  prob     : edmonds_canopy_prob_2016_corrected.tif   @ thresh 0.509

  valid px (prob + both refs present) : 1,348,637,499

  AGREEMENT PARTITION:
    BOTH say canopy            352,048,981  (26.10%)  <- trustworthy positive
    BOTH say non-canopy        793,532,306  (58.84%)  <- trustworthy negative
    NDVI only                  156,908,723  (11.63%)  \
    C-CAP only                  46,147,489  ( 3.42%)  / DISAGREE = UNMEASURABLE
    -> refs disagree on        203,056,212  (15.06%) of all valid pixels

  MODEL SCORED ON THE BOTH-AGREE SUBSET ONLY (reference noise removed):
    recall    0.9486
    precision 0.9254
    TP 333,968,589  FN 18,080,392  FP 26,910,393

  WHERE THE HEADLINE C-CAP MISS ACTUALLY LIVES:
    C-CAP canopy px         398,196,470
    model missed (FN)       51,054,768
      ...of which BOTH refs agree it IS canopy : 18,080,392 (35.4%)  <- REAL MISS
      ...of which refs DISAGREE                : 32,974,376 (64.6%)  <- UNMEASURABLE

  MODEL CANOPY-CALL RATE BY PARTITION (diagnostic):
    both_canopy         94.86%   (333,968,589 / 352,048,981)
    ndvi_only           64.84%   (101,736,362 / 156,908,723)
    ccap_only           28.55%   (13,173,113 / 46,147,489)
    both_noncanopy       3.39%   (26,910,393 / 793,532,306)

  READ:
    * The BOTH-AGREE recall is the honest number with most reference
      noise removed. If it is much higher than the raw C-CAP recall,
      the references — not the model — carry a large share of the gap.
    * A high 'ccap_only' canopy-call rate means the model sides with
      NDVI; a high 'ndvi_only' rate means it sides with C-CAP.
    * CAVEAT: the NDVI reference uses a ~2016 CHM regardless of year,
      so its height test is temporally offset (see ndvi_ref_*.txt).